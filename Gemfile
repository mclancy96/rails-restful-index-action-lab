source 'https://rubygems.org'

ruby '3.3.5'

# Rails 7.1 and core dependencies
gem 'nokogiri', '~> 1.11'
gem 'puma', '~> 6.0'
gem 'rails', '~> 7.1.0'
gem 'sqlite3', '~> 1.4'

# Asset pipeline (optional, not using sprockets by default in Rails 7)
# gem 'sprockets-rails'

# Modern JS/CSS (optional, for students to add if needed)
# gem 'importmap-rails'
# gem 'jsbundling-rails'
# gem 'cssbundling-rails'

# JSON APIs
gem 'jbuilder', '~> 2.0'

group :development, :test do
  gem 'byebug'
  gem 'capybara'
  gem 'database_cleaner'
  gem 'ostruct'
  gem 'pry'
  gem 'rspec-rails', '~> 6.0'
  gem 'rubocop', require: false
  gem 'spring'
  gem 'webrick'
end
